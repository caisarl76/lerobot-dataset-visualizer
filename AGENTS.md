# Visualizer workspace

Use `/home/jihun/work/lerobot-dataset-visualizer` as the primary local checkout.
Create future visualizer Git worktrees under this repository's `.worktrees/`
directory. The annotation services run from the primary checkout; preserve its
unfinished edits when integrating a feature branch.

See `deployment/README.md` for annotation service startup and runtime settings.
